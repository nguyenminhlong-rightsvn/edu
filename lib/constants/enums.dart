enum ParentBottomAppBarType {
  home,
  filteredSearch,
  scout,
  profile,
}

enum SchoolBottomAppBarType {
  visitorLog,
  filteredSearch,
  favorites,
  scout,
  profile,
}

enum Role { parent, school }
